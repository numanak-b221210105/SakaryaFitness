@{
    ViewData["Title"] = "AI Antrenör";
}

<div class="container py-4">
    <div class="text-center mb-4">
        <h1 class="display-5 fw-bold text-primary">
            <i class="bi bi-robot"></i> AI Akıllı Antrenör
        </h1>
        @if (ViewBag.Plan == null)
        {
            <p class="lead text-muted">Vücut ölçülerinizi girin, size özel planınızı ve <strong>dönüşüm görselinizi</strong> hazırlayalım.</p>
        }
    </div>

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger shadow-sm fw-bold">
            <i class="bi bi-exclamation-triangle-fill me-2 fs-5" style="vertical-align: middle;"></i> @ViewBag.Error
        </div>
    }

    @if (ViewBag.Plan != null)
    {
        <div class="card border-0 shadow-lg rounded-4 overflow-hidden mb-5">
            <div class="card-header bg-success text-white p-3 text-center">
                <h2 class="h4 mb-0 fw-bold"><i class="bi bi-check-circle-fill me-2"></i> Analiz Başarıyla Tamamlandı!</h2>
            </div>
            <div class="card-body p-4 bg-light bg-opacity-50">
                
                <div class="row g-4 mb-4">
                    <div class="col-md-5 col-lg-4 text-center border-end-md">
                        <h5 class="fw-bold text-primary mb-3">
                            <i class="bi bi-image"></i> Hedeflenen Görünüm
                        </h5>
                        <div class="card shadow-sm border-0 mb-3">
                            <img src="@ViewBag.ImageUrl" class="card-img-top rounded img-fluid" style="max-height: 350px; object-fit: cover;" alt="Hedef Görünüm">
                             <div class="card-footer bg-white p-2 border-0">
                                <small class="text-muted fst-italic">AI tarafından öngörülen temsili değişim</small>
                            </div>
                        </div>
                        <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                             <button onclick="window.print()" class="btn btn-outline-dark btn-sm rounded-pill px-4">
                                <i class="bi bi-printer me-2"></i> Çıktı Al
                            </button>
                             <a href="/YapayZeka" class="btn btn-primary btn-sm rounded-pill px-4">
                                <i class="bi bi-arrow-repeat me-2"></i> Yeni Analiz
                            </a>
                        </div>
                    </div>

                    <div class="col-md-7 col-lg-8 d-flex flex-column justify-content-center">
                         <div class="ps-md-4">
                            <h4 class="fw-bold text-success mb-4">📋 Kişisel Analiz Özeti</h4>
                            <div class="bg-white p-4 rounded-3 shadow-sm border">
                                <div class="row text-center g-3">
                                    <div class="col-4 border-end">
                                        <h6 class="text-muted mb-1">YAŞ</h6>
                                        <span class="fs-4 fw-bold text-dark">@ViewBag.UserAge</span>
                                    </div>
                                    <div class="col-4 border-end">
                                        <h6 class="text-muted mb-1">BOY</h6>
                                        <span class="fs-4 fw-bold text-dark">@ViewBag.UserHeight <small class="fs-6">cm</small></span>
                                    </div>
                                    <div class="col-4">
                                        <h6 class="text-muted mb-1">KİLO</h6>
                                        <span class="fs-4 fw-bold text-dark">@ViewBag.UserWeight <small class="fs-6">kg</small></span>
                                    </div>
                                </div>
                            </div>
                         </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <h5 class="fw-bold text-dark border-bottom pb-3 mb-3">
                            <i class="bi bi-file-earmark-text me-2"></i> 4 Haftalık Detaylı Planınız
                        </h5>
                        <div class="plan-content bg-white p-4 rounded-3 shadow-sm border" 
                             style="max-height: 500px; overflow-y: auto; line-height: 1.6; font-size: 0.95rem;">
                            @Html.Raw(ViewBag.Plan)
                        </div>
                    </div>
                </div>

            </div>
        </div>
    }
    else
    {
        <div class="row justify-content-center">
            <div class="col-lg-8 col-xl-7">
                <div class="card shadow rounded-4 overflow-hidden">
                    <div class="card-header bg-gradient bg-primary text-white p-4 text-center">
                        <h3 class="mb-1 fw-bold">
                            <i class="bi bi-sliders"></i> Bilgilerinizi Girin
                        </h3>
                    </div>
                    <div class="card-body p-4 p-md-5">
                        <form asp-action="GeneratePlan" method="post" onsubmit="document.getElementById('loadingSpinner').style.display='block'; document.getElementById('submitBtn').disabled=true;">
                            
                            <div class="row g-3 mb-4">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Yaşınız</label>
                                    <input type="number" name="age" class="form-control form-control-lg" required placeholder="Örn: 25" min="10" max="100" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Cinsiyet</label>
                                    <select name="gender" class="form-select form-select-lg">
                                        <option value="Erkek">Erkek</option>
                                        <option value="Kadın">Kadın</option>
                                    </select>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Boy (cm)</label>
                                    <input type="number" name="height" class="form-control form-control-lg" required placeholder="Örn: 175" min="100" max="250" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Kilo (kg)</label>
                                    <input type="number" name="weight" class="form-control form-control-lg" required placeholder="Örn: 75" min="30" max="200" />
                                </div>
                            </div>

                            <div class="mb-4">
                                <label class="form-label fw-bold d-block">Hedefiniz Nedir?</label>
                                <div class="row g-2">
                                    <div class="col-md-4">
                                        <input type="radio" class="btn-check" name="goal" id="goal1" value="kilo_ver" checked>
                                        <label class="btn btn-outline-primary w-100 h-100 p-3 text-center rounded-3" for="goal1">
                                            Kilo Vermek
                                        </label>
                                    </div>
                                    <div class="col-md-4">
                                        <input type="radio" class="btn-check" name="goal" id="goal2" value="kas_yap">
                                        <label class="btn btn-outline-success w-100 h-100 p-3 text-center rounded-3" for="goal2">
                                            Kas Yapmak
                                        </label>
                                    </div>
                                    <div class="col-md-4">
                                        <input type="radio" class="btn-check" name="goal" id="goal3" value="form_koru">
                                        <label class="btn btn-outline-info w-100 h-100 p-3 text-center rounded-3" for="goal3">
                                            Form Korumak
                                        </label>
                                    </div>
                                </div>
                            </div>

                            <div class="d-grid gap-2">
                                <button id="submitBtn" type="submit" class="btn btn-primary btn-lg rounded-pill py-3 fw-bold fs-5 shadow-sm">
                                    <i class="bi bi-stars"></i> Planı ve Görseli Oluştur
                                </button>
                            </div>

                            <div id="loadingSpinner" class="text-center mt-4" style="display:none;">
                                <div class="spinner-border text-primary" role="status"></div>
                                <p class="mt-2 text-muted fw-bold">Yapay zeka planınızı ve görselinizi hazırlıyor...</p>
                            </div>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    }
</div>