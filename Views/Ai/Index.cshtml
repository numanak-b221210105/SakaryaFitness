@{
    ViewData["Title"] = "AI Antrenör";
}

<div class="container py-5">
    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold text-primary">
            <i class="bi bi-robot"></i> AI Akıllı Antrenör
        </h1>
        <p class="lead text-muted">Vücut ölçülerinizi girin, size özel planınızı ve <strong>dönüşüm görselinizi</strong> hazırlayalım.</p>
    </div>

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger text-center">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> @ViewBag.Error
        </div>
    }

    @if (ViewBag.Plan != null)
    {
        <div class="row justify-content-center">
            <div class="col-lg-10"> <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                    <div class="card-header bg-success text-white p-4 text-center">
                        <h2 class="h3 mb-0"><i class="bi bi-check-circle-fill me-2"></i> Analiz Tamamlandı!</h2>
                    </div>
                    <div class="card-body p-0">
                        <div class="row g-0">
                            <div class="col-lg-7 p-5">
                                <h4 class="fw-bold mb-3 text-success">📋 Senin İçin Hazırlanan Plan</h4>
                                <div class="mb-4">
                                    <span class="badge bg-light text-dark border p-2 me-1">Yaş: @ViewBag.UserAge</span>
                                    <span class="badge bg-light text-dark border p-2 me-1">Boy: @ViewBag.UserHeight cm</span>
                                    <span class="badge bg-light text-dark border p-2">Kilo: @ViewBag.UserWeight kg</span>
                                </div>
                                <div class="plan-content fs-6" style="line-height: 1.7;">
                                    @Html.Raw(ViewBag.Plan)
                                </div>
                            </div>

                            <div class="col-lg-5 bg-light d-flex flex-column align-items-center justify-content-center p-4 border-start">
                                <h5 class="fw-bold text-primary mb-3 text-center">
                                    <i class="bi bi-image"></i> Hedeflenen Görünüm
                                </h5>
                                <div class="card shadow-sm border-0 w-100">
                                    <img src="@ViewBag.ImageUrl" class="card-img-top rounded" alt="Hedef Görünüm">
                                    <div class="card-body text-center p-2">
                                        <small class="text-muted fst-italic">AI tarafından öngörülen değişim</small>
                                    </div>
                                </div>
                                <div class="mt-4 text-center">
                                     <button onclick="window.print()" class="btn btn-outline-dark btn-sm rounded-pill px-3">
                                        <i class="bi bi-printer me-2"></i> Çıktı Al
                                    </button>
                                     <a href="/YapayZeka" class="btn btn-primary btn-sm rounded-pill px-3 ms-2">
                                        <i class="bi bi-arrow-repeat me-2"></i> Yeni Analiz
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="row justify-content-center">
            <div class="col-lg-8 col-xl-7">
                <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
                    <div class="card-header bg-gradient bg-primary text-white p-4 text-center">
                        <h3 class="mb-1 fw-bold">
                            <i class="bi bi-sliders"></i> Bilgilerinizi Girin
                        </h3>
                    </div>
                    <div class="card-body p-4 p-md-5">
                        <form asp-action="GeneratePlan" method="post" onsubmit="document.getElementById('loadingSpinner').style.display='block'; document.getElementById('submitBtn').disabled=true;">
                            
                            <div class="row g-3 mb-4">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Yaşınız</label>
                                    <input type="number" name="age" class="form-control form-control-lg" required value="25" min="10" max="100" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Cinsiyet</label>
                                    <select name="gender" class="form-select form-select-lg">
                                        <option value="Erkek">Erkek</option>
                                        <option value="Kadın">Kadın</option>
                                    </select>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Boy (cm)</label>
                                    <input type="number" name="height" class="form-control form-control-lg" required value="175" min="100" max="250" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Kilo (kg)</label>
                                    <input type="number" name="weight" class="form-control form-control-lg" required value="75" min="30" max="200" />
                                </div>
                            </div>

                            <div class="mb-4">
                                <label class="form-label fw-bold d-block">Hedefiniz Nedir?</label>
                                <div class="row g-2">
                                    <div class="col-md-4">
                                        <input type="radio" class="btn-check" name="goal" id="goal1" value="kilo_ver" checked>
                                        <label class="btn btn-outline-primary w-100 h-100 p-3 text-center rounded-3" for="goal1">
                                            Kilo Vermek
                                        </label>
                                    </div>
                                    <div class="col-md-4">
                                        <input type="radio" class="btn-check" name="goal" id="goal2" value="kas_yap">
                                        <label class="btn btn-outline-success w-100 h-100 p-3 text-center rounded-3" for="goal2">
                                            Kas Yapmak
                                        </label>
                                    </div>
                                    <div class="col-md-4">
                                        <input type="radio" class="btn-check" name="goal" id="goal3" value="form_koru">
                                        <label class="btn btn-outline-info w-100 h-100 p-3 text-center rounded-3" for="goal3">
                                            Form Korumak
                                        </label>
                                    </div>
                                </div>
                            </div>

                            <div class="d-grid gap-2">
                                <button id="submitBtn" type="submit" class="btn btn-primary btn-lg rounded-pill py-3 fw-bold fs-5 shadow-sm">
                                    <i class="bi bi-stars"></i> Planı ve Görseli Oluştur
                                </button>
                            </div>

                            <div id="loadingSpinner" class="text-center mt-4" style="display:none;">
                                <div class="spinner-border text-primary" role="status"></div>
                                <p class="mt-2 text-muted fw-bold">Yapay zeka planınızı ve görselinizi hazırlıyor...</p>
                            </div>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    }
</div>